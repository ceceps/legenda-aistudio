{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm --filter=@legenda/shared-types build && pnpm --filter=@legenda/api prisma generate && pnpm --filter=@legenda/api build",
    "startCommand": "node apps/api/dist/index.js",
    "watchPatterns": ["apps/api/**", "packages/shared-types/**"]
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
